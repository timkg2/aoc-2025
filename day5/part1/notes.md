An element is fresh if the ID in a range
get all ranges with lower bound lower than ID
    and upper bound larger than ID
if range exists, element is fresh

balanced search tree for fast lookups?
but problem space seems to be small.

